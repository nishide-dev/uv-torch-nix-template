# Copier template configuration for uv-torch-nix-template
# This is an extension template that adds PyTorch/CUDA support to uv-nix-template

_min_copier_version: "9.0.0"

# ============================================================================
# User questions - Project基本情報
# ============================================================================

project_name:
  type: str
  help: "プロジェクト名を入力してください (Enterでディレクトリ名を使用)"
  default: "{{ _copier_conf.dst_path.name }}"

package_name:
  type: str
  help: "パッケージ名を入力してください (Pythonのインポート名、通常はスネークケース)"
  default: "{{ project_name | replace('-', '_') }}"

description:
  type: str
  help: "プロジェクトの短い説明を入力してください"
  default: "A PyTorch/CUDA deep learning project"

author_name:
  type: str
  help: "作者名を入力してください"
  default: "Your Name"

author_email:
  type: str
  help: "作者のメールアドレスを入力してください"
  default: "your.email@example.com"

python_version:
  type: str
  help: "対象とする最小Pythonバージョンを選択してください"
  default: "3.12"
  choices:
    - "3.10"
    - "3.11"
    - "3.12"
    - "3.13"

# ============================================================================
# User questions - 開発ツール設定
# ============================================================================

use_ruff:
  type: bool
  help: "Ruff (高速linter/formatter) を使用しますか？"
  default: true

use_ty:
  type: bool
  help: "ty (高速型チェッカー by Astral) を使用しますか？"
  default: true

use_pytest:
  type: bool
  help: "Pytest (テストフレームワーク) を使用しますか？"
  default: true

use_github_actions:
  type: bool
  help: "GitHub Actions CI/CDを設定しますか？"
  default: true

use_nix:
  type: bool
  help: "Nix + direnv による開発環境管理を使用しますか？"
  default: true

setup_direnv_hook:
  type: bool
  help: "シェル設定ファイル（~/.bashrc or ~/.zshrc）にdirenv hookを自動追加しますか？"
  default: false
  when: "{{ use_nix }}"

# ============================================================================
# User questions - PyTorch/CUDA configuration
# ============================================================================
#
# 【CUDA/PyTorchバージョン対応表】
# ┌─────────────┬─────────────┬──────────────┬─────────────────────┐
# │ PyTorch     │ CUDA        │ cuDNN        │ 推奨環境            │
# ├─────────────┼─────────────┼──────────────┼─────────────────────┤
# │ 2.5.1       │ 12.4, 12.1  │ 9.1.0        │ RTX 40xx (Ada)      │
# │ 2.4.1       │ 12.1, 11.8  │ 8.9.7        │ RTX 30xx (Ampere)   │
# │ 2.3.1       │ 12.1, 11.8  │ 8.9.2        │ RTX 30xx (Ampere)   │
# │ 2.2.2       │ 12.1, 11.8  │ 8.9.2        │ 汎用                │
# │ 2.1.2       │ 12.1, 11.8  │ 8.9.2        │ 古いGPU対応         │
# └─────────────┴─────────────┴──────────────┴─────────────────────┘
#
# 【注意事項】
# - NixOS上で使用する場合は docs/NIX_SETUP.md を必ず参照してください
# - nixpkgsで利用可能なCUDAバージョンは限られています（11.8, 12.1等）
# - CUDA 12.4以降は nixpkgs の対応状況を確認してください
# - GPUアーキテクチャに応じて適切なCUDAバージョンを選択してください
#
# 詳細: https://pytorch.org/get-started/locally/
# ============================================================================

pytorch_version:
  type: str
  help: "PyTorchバージョンを入力してください（例: 2.5.1, 2.4.1, 2.3.1）"
  default: "2.5.1"

cuda_version:
  type: str
  help: "CUDAバージョンを入力してください（例: 12.4, 12.1, 11.8）｜NixOS: docs/NIX_SETUP.md参照"
  default: "12.4"

cudnn_version:
  type: str
  help: "cuDNNバージョンを入力してください（例: 9.1.0, 8.9.7）"
  default: "9.1.0"

pytorch_cuda_arch:
  type: str
  help: "PyTorch CUDA architectureを入力してください（cu124, cu121, cu118など。空白の場合はcuda_versionから自動生成）"
  default: ""

use_torchvision:
  type: bool
  help: "torchvisionを含めますか？"
  default: true

use_torchaudio:
  type: bool
  help: "torchaudioを含めますか？"
  default: false

additional_cuda_libs:
  type: str
  help: "追加のCUDAライブラリ（カンマ区切り、例: nccl,cutlass,libcusparse）｜分散学習やスパース演算に必要"
  default: ""

# ============================================================================
# File/directory exclusion settings
# ============================================================================

_exclude:
  - "copier.yml"
  - "copier.yaml"
  - ".git"
  - ".venv"
  - "__pycache__"
  - "*.pyc"
  - ".pytest_cache"
  - ".mypy_cache"
  - ".ruff_cache"
  - "uv.lock"
  - "dist"
  - "build"
  - "*.egg-info"
  - "{% if not use_ruff %}ruff.toml.jinja{% endif %}"  # Ruff未使用の場合は除外
  - "{% if not use_nix %}flake.nix.jinja{% endif %}"  # Nix未使用の場合は除外
  - "{% if not use_nix %}.envrc{% endif %}"  # Nix未使用の場合は除外
  - "{% if not use_nix %}docs{% endif %}"  # Nix未使用の場合はdocsディレクトリ全体を除外
  - "{% if not use_github_actions %}.github{% endif %}"  # GitHub Actions未使用の場合は除外

# ============================================================================
# Post-generation tasks
# ============================================================================

_tasks:
  - "git init -b main"
  - "git add ."
  - |
    {% if use_nix and setup_direnv_hook %}
    # シェル設定ファイルへのdirenv hook追加
    for rcfile in ~/.bashrc ~/.zshrc; do
      if [ -f "$rcfile" ] && ! grep -q "direnv hook" "$rcfile"; then
        shell=$(basename "$rcfile" | sed 's/^\.//;s/rc$//')
        echo 'eval "$(direnv hook '"$shell"')"' >> "$rcfile"
        echo "✅ Added direnv hook to $rcfile"
      fi
    done
    {% endif %}
  - "uv venv"
  - "uv lock"
  - "git add uv.lock"
  - "uv sync"
  - |
    set -e
    {% if use_ruff %}
    echo "🔍 Ruff check..."
    uv run ruff check . --fix
    echo "✨ Ruff format..."
    uv run ruff format .
    {% endif %}
    {% if use_ty %}
    echo "🔎 Type checking..."
    uv run ty check
    {% endif %}
    {% if use_pytest %}
    echo "🧪 Running tests..."
    uv run pytest
    {% endif %}
    echo "✅ All quality checks passed!"

  # 成功メッセージ
  - |
    echo "
    ============================================================
    🎉 PyTorch/CUDA プロジェクトが正常に作成されました！
    ============================================================

    📦 Project: {{ project_name }}
    🔥 PyTorch: {{ pytorch_version }} + CUDA {{ cuda_version }}

    次のステップ:
      1. プロジェクトディレクトリに移動:
         cd {{ project_name }}

      2. 初回コミット (ファイルは既にステージング済み):
         git commit -m \"chore: initial commit\"

      3. 開発を開始 (仮想環境は既にセットアップ済み):
         uv add <package-name>      # 依存関係の追加
         uv run pytest              # テストの実行
         uv run ruff format .       # コードフォーマット
         uv run ty check            # 型チェック

      4. CUDA動作確認:
         uv run python -c 'import torch; print(f\"CUDA: {torch.cuda.is_available()}\")'

    {% if use_nix %}
    📚 NixOS users: docs/NIX_SETUP.md を参照してください
    {% endif %}
    📖 詳細: docs/CUDA_SETUP.md

    ============================================================
    "

# ============================================================================
# Template metadata
# ============================================================================

_metadata:
  template_version: "0.1.0"
  base_template: "uv-nix-template"
  description: "PyTorch/CUDA extension for uv-nix-template"
